import PortalIcon from "@components/PortalIcon/PortalIcon";
const LoadingPanel = () => {
  return (
    <div className="w-full h-full flex items-center justify-center window">
      <PortalIcon width={400} height={400}></PortalIcon>
    </div>
  );
};

export default LoadingPanel;
